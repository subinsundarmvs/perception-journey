# Virtual environment — machine-specific, regenerable from requirements.txt. Never commit.
venv/
.venv/

# Python cache
__pycache__/
*.pyc
*.pyo

# Jupyter checkpoints
.ipynb_checkpoints/

# macOS clutter (your Mac scatters these everywhere)
.DS_Store

# Editor
.vscode/

# Large / generated media — keep the repo light. Un-ignore specific samples if you want them tracked.
*.mp4
*.avi
# (leave sample images tracked so demos render on GitHub; comment the next line out if you add big ones)
# *.png
